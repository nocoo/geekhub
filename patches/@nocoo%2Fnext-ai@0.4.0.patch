diff --git a/dist/react.js b/dist/react.js
index 3d0ba832d187a41e3638608d265748d4167f2ef7..c325d5a213de7c5b09bc3e31435daf04bfb9b963 100644
--- a/dist/react.js
+++ b/dist/react.js
@@ -1,6 +1,23 @@
 "use client";
 "use client";
-
+
+// GeekHub integration: retain next-ai's configuration state, use Basalt controls.
+import { Input as BasaltInput } from "@nocoo/basalt/components/input";
+import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@nocoo/basalt/components/select";
+import { Children } from "react";
+import { jsx as controlJsx, jsxs as controlJsxs } from "react/jsx-runtime";
+function BasaltSelect({ id, value, onChange, disabled, className, children }) {
+  const options = Children.toArray(children).filter((child) => child.props.value !== undefined);
+  const placeholder = options.find((option) => option.props.value === "")?.props.children;
+  return controlJsxs(Select, {
+    value, disabled, onValueChange: (value) => onChange({ target: { value } }),
+    children: [
+      controlJsx(SelectTrigger, { id, className, children: controlJsx(SelectValue, { placeholder }) }),
+      controlJsx(SelectContent, { className: "select-options", children: options.filter((option) => option.props.value !== "").map((option) => controlJsx(SelectItem, { value: option.props.value, disabled: option.props.disabled, children: option.props.children }, option.props.value)) })
+    ]
+  });
+}
+
 // src/react/context.tsx
 import {
   createContext,
@@ -257,7 +274,7 @@ function ApiKeyInput({
   };
   return /* @__PURE__ */ jsxs("div", { className: cn("relative", className), children: [
     /* @__PURE__ */ jsx2(
-      "input",
+      BasaltInput,
       {
         id,
         type: visible ? "text" : "password",
@@ -268,16 +285,7 @@ function ApiKeyInput({
         placeholder: "Enter API key...",
         disabled,
         readOnly: showPlaceholder,
-        className: cn(
-          "flex h-10 w-full rounded-lg px-3 py-2 pr-10",
-          "text-sm font-mono",
-          "bg-[hsl(var(--input))] text-[hsl(var(--foreground))]",
-          "border border-[hsl(var(--border))]",
-          "placeholder:text-[hsl(var(--muted-foreground))]",
-          "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[hsl(var(--primary))]",
-          "disabled:cursor-not-allowed disabled:opacity-50",
-          showPlaceholder && "cursor-pointer text-[hsl(var(--muted-foreground))]"
-        )
+        className: "font-mono pr-10"
       }
     ),
     /* @__PURE__ */ jsx2(
@@ -362,7 +370,7 @@ function ModelSelect({
   if (isCustomProvider || isCustom || isValueCustomModel) {
     return /* @__PURE__ */ jsxs2("div", { className: cn("flex gap-2", className), children: [
       /* @__PURE__ */ jsx3(
-        "input",
+        BasaltInput,
         {
           id,
           type: "text",
@@ -370,15 +378,7 @@ function ModelSelect({
           onChange: (e) => onChange(e.target.value),
           placeholder: "Enter model name...",
           disabled,
-          className: cn(
-            "flex h-10 flex-1 rounded-lg px-3 py-2",
-            "text-sm",
-            "bg-[hsl(var(--input))] text-[hsl(var(--foreground))]",
-            "border border-[hsl(var(--border))]",
-            "placeholder:text-[hsl(var(--muted-foreground))]",
-            "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[hsl(var(--primary))]",
-            "disabled:cursor-not-allowed disabled:opacity-50"
-          )
+          className: undefined
         }
       ),
       !isCustomProvider && /* @__PURE__ */ jsx3(
@@ -403,7 +403,7 @@ function ModelSelect({
     ] });
   }
   return /* @__PURE__ */ jsxs2(
-    "select",
+    BasaltSelect,
     {
       id,
       value,
@@ -416,15 +416,7 @@ function ModelSelect({
         }
       },
       disabled,
-      className: cn(
-        "flex h-10 w-full rounded-lg px-3 py-2",
-        "text-sm",
-        "bg-[hsl(var(--input))] text-[hsl(var(--foreground))]",
-        "border border-[hsl(var(--border))]",
-        "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[hsl(var(--primary))]",
-        "disabled:cursor-not-allowed disabled:opacity-50",
-        className
-      ),
+      className: className,
       children: [
         /* @__PURE__ */ jsx3("option", { value: "", children: "Select model..." }),
         models.map((model) => /* @__PURE__ */ jsx3("option", { value: model, children: model }, model)),
@@ -447,21 +439,13 @@ function ProviderSelect({
   const registry = useProviderRegistry();
   const providers = registry.getAll();
   return /* @__PURE__ */ jsxs3(
-    "select",
+    BasaltSelect,
     {
       id,
       value,
       onChange: (e) => onChange(e.target.value),
       disabled,
-      className: cn(
-        "flex h-10 w-full rounded-lg px-3 py-2",
-        "text-sm",
-        "bg-[hsl(var(--input))] text-[hsl(var(--foreground))]",
-        "border border-[hsl(var(--border))]",
-        "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[hsl(var(--primary))]",
-        "disabled:cursor-not-allowed disabled:opacity-50",
-        className
-      ),
+      className: className,
       children: [
         /* @__PURE__ */ jsx4("option", { value: "", children: "Select provider..." }),
         providers.map((provider) => /* @__PURE__ */ jsx4("option", { value: provider.id, children: provider.label }, provider.id)),
@@ -621,7 +605,7 @@ function AiSettingsPanel({
                 }
               ),
               /* @__PURE__ */ jsx5(
-                "input",
+                BasaltInput,
                 {
                   id: "ai-base-url-input",
                   type: "url",
@@ -629,15 +613,7 @@ function AiSettingsPanel({
                   onChange: (e) => setBaseURL(e.target.value),
                   placeholder: "https://api.example.com/v1",
                   disabled: saving,
-                  className: cn(
-                    "flex h-10 w-full rounded-lg px-3 py-2",
-                    "text-sm",
-                    "bg-[hsl(var(--input))] text-[hsl(var(--foreground))]",
-                    "border border-[hsl(var(--border))]",
-                    "placeholder:text-[hsl(var(--muted-foreground))]",
-                    "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[hsl(var(--primary))]",
-                    "disabled:cursor-not-allowed disabled:opacity-50"
-                  )
+                  className: undefined
                 }
               )
             ] }),
@@ -651,20 +627,13 @@ function AiSettingsPanel({
                 }
               ),
               /* @__PURE__ */ jsxs4(
-                "select",
+                BasaltSelect,
                 {
                   id: "ai-sdk-type-select",
                   value: sdkType,
                   onChange: (e) => setSdkType(e.target.value),
                   disabled: saving,
-                  className: cn(
-                    "flex h-10 w-full rounded-lg px-3 py-2",
-                    "text-sm",
-                    "bg-[hsl(var(--input))] text-[hsl(var(--foreground))]",
-                    "border border-[hsl(var(--border))]",
-                    "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[hsl(var(--primary))]",
-                    "disabled:cursor-not-allowed disabled:opacity-50"
-                  ),
+                  className: undefined,
                   children: [
                     /* @__PURE__ */ jsx5("option", { value: "", children: "Select SDK type..." }),
                     /* @__PURE__ */ jsx5("option", { value: "anthropic", children: "Anthropic" }),
@@ -761,17 +730,11 @@ function PromptTemplateSelector({
   const selectedTemplate = value ? registry.get(value) : void 0;
   return /* @__PURE__ */ jsxs5("div", { className: cn("space-y-3", className), children: [
     /* @__PURE__ */ jsxs5(
-      "select",
+      BasaltSelect,
       {
         value,
         onChange: (e) => onChange(e.target.value),
-        className: cn(
-          "flex h-10 w-full rounded-lg px-3 py-2",
-          "text-sm",
-          "bg-[hsl(var(--input))] text-[hsl(var(--foreground))]",
-          "border border-[hsl(var(--border))]",
-          "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[hsl(var(--primary))]"
-        ),
+        className: className,
         children: [
           /* @__PURE__ */ jsx6("option", { value: "", children: "Select template..." }),
           templates.map((template) => /* @__PURE__ */ jsx6("option", { value: template.id, children: template.name }, template.id))
diff --git a/dist/server-next.d.ts b/dist/server-next.d.ts
index 97037b4540307bf27cf2cc4770f702a2c64a39f5..d1224099e562261219bfe49f5c25223f3befac87 100644
--- a/dist/server-next.d.ts
+++ b/dist/server-next.d.ts
@@ -70,9 +70,15 @@ interface AiConfigError {
  * - 缺省 / `"apiKey"`：按 SDK 默认行为（Anthropic 走 `x-api-key`，
  *   OpenAI 走 `Authorization: Bearer`）。
  */
-declare function createAiClient(config: AiConfig): _ai_sdk_openai.OpenAIProvider | _ai_sdk_anthropic.AnthropicProvider;
+interface AiClientOptions {
+    /** Per-client transport; never replaces global fetch. */
+    fetch?: _ai_sdk_openai.OpenAIProviderSettings["fetch"];
+    /** Defaults to Responses for compatibility with existing consumers. */
+    openaiApi?: "responses" | "chat";
+}
+declare function createAiClient(config: AiConfig, options?: AiClientOptions): _ai_sdk_openai.OpenAIProvider | _ai_sdk_anthropic.AnthropicProvider;
 /** 创建 AI 模型实例（用于 Vercel AI SDK） */
-declare function createAiModel(config: AiConfig): _ai_sdk_provider.LanguageModelV4;
+declare function createAiModel(config: AiConfig, options?: AiClientOptions): _ai_sdk_provider.LanguageModelV4;
 
 /** AI Helper 选项 */
 interface AiHelperOptions {
@@ -193,4 +199,4 @@ declare class PromptTemplateRegistry {
     build(templateId: string, variables: TemplateVariables, customSections?: Record<string, string>): string;
 }
 
-export { type AiCompletionResult, type AiConfig, type AiConfigError, type AiHelperOptions, PromptTemplateRegistry, type TemplateVariables, aiChat, aiComplete, aiCompleteWithRetry, aiStream, createAiClient, createAiModel, expandTemplate, resolveAiConfig, validateAiConfig, validateTestConfig };
+export { type AiClientOptions, type AiCompletionResult, type AiConfig, type AiConfigError, type AiHelperOptions, PromptTemplateRegistry, type TemplateVariables, aiChat, aiComplete, aiCompleteWithRetry, aiStream, createAiClient, createAiModel, expandTemplate, resolveAiConfig, validateAiConfig, validateTestConfig };
diff --git a/dist/server-next.js b/dist/server-next.js
index a749df2e0fbdee3931ba769896b0dc9aab8e5fe4..b57e542a8caef68eeda92171c058a64b454230d0 100644
--- a/dist/server-next.js
+++ b/dist/server-next.js
@@ -4,31 +4,22 @@ import "server-only";
 // src/server/client.ts
 import { createAnthropic } from "@ai-sdk/anthropic";
 import { createOpenAI } from "@ai-sdk/openai";
-function createAiClient(config) {
+function createAiClient(config, options = {}) {
   const { baseURL, apiKey, sdkType, authType } = config;
-  const useBearer = authType === "bearer";
+  const transport = { baseURL, fetch: options.fetch };
   if (sdkType === "openai") {
-    if (useBearer) {
-      return createOpenAI({
-        baseURL,
-        apiKey,
-        headers: { Authorization: `Bearer ${apiKey}` }
-      });
-    }
-    return createOpenAI({ baseURL, apiKey });
+    return createOpenAI({ ...transport, apiKey });
   }
-  if (useBearer) {
-    return createAnthropic({
-      baseURL,
-      apiKey,
-      headers: { Authorization: `Bearer ${apiKey}` }
-    });
-  }
-  return createAnthropic({ baseURL, apiKey });
+  return createAnthropic({
+    ...transport,
+    ...(authType === "bearer" ? { authToken: apiKey } : { apiKey })
+  });
 }
-function createAiModel(config) {
-  const client = createAiClient(config);
-  return client(config.model);
+function createAiModel(config, options = {}) {
+  const client = createAiClient(config, options);
+  return options.openaiApi === "chat" && "chat" in client
+    ? client.chat(config.model)
+    : client(config.model);
 }
 
 // src/server/helpers.ts
@@ -329,4 +320,3 @@ export {
   validateAiConfig,
   validateTestConfig
 };
-//# sourceMappingURL=server-next.js.map
\ No newline at end of file
diff --git a/dist/server.d.ts b/dist/server.d.ts
index 97037b4540307bf27cf2cc4770f702a2c64a39f5..d1224099e562261219bfe49f5c25223f3befac87 100644
--- a/dist/server.d.ts
+++ b/dist/server.d.ts
@@ -70,9 +70,15 @@ interface AiConfigError {
  * - 缺省 / `"apiKey"`：按 SDK 默认行为（Anthropic 走 `x-api-key`，
  *   OpenAI 走 `Authorization: Bearer`）。
  */
-declare function createAiClient(config: AiConfig): _ai_sdk_openai.OpenAIProvider | _ai_sdk_anthropic.AnthropicProvider;
+interface AiClientOptions {
+    /** Per-client transport; never replaces global fetch. */
+    fetch?: _ai_sdk_openai.OpenAIProviderSettings["fetch"];
+    /** Defaults to Responses for compatibility with existing consumers. */
+    openaiApi?: "responses" | "chat";
+}
+declare function createAiClient(config: AiConfig, options?: AiClientOptions): _ai_sdk_openai.OpenAIProvider | _ai_sdk_anthropic.AnthropicProvider;
 /** 创建 AI 模型实例（用于 Vercel AI SDK） */
-declare function createAiModel(config: AiConfig): _ai_sdk_provider.LanguageModelV4;
+declare function createAiModel(config: AiConfig, options?: AiClientOptions): _ai_sdk_provider.LanguageModelV4;
 
 /** AI Helper 选项 */
 interface AiHelperOptions {
@@ -193,4 +199,4 @@ declare class PromptTemplateRegistry {
     build(templateId: string, variables: TemplateVariables, customSections?: Record<string, string>): string;
 }
 
-export { type AiCompletionResult, type AiConfig, type AiConfigError, type AiHelperOptions, PromptTemplateRegistry, type TemplateVariables, aiChat, aiComplete, aiCompleteWithRetry, aiStream, createAiClient, createAiModel, expandTemplate, resolveAiConfig, validateAiConfig, validateTestConfig };
+export { type AiClientOptions, type AiCompletionResult, type AiConfig, type AiConfigError, type AiHelperOptions, PromptTemplateRegistry, type TemplateVariables, aiChat, aiComplete, aiCompleteWithRetry, aiStream, createAiClient, createAiModel, expandTemplate, resolveAiConfig, validateAiConfig, validateTestConfig };
diff --git a/dist/server.js b/dist/server.js
index 3f6be283f583003b830a5cec828bdbe5d2c0f1db..dbd2bd233d055cb22eb4386b416db29bb892702c 100644
--- a/dist/server.js
+++ b/dist/server.js
@@ -1,31 +1,22 @@
 // src/server/client.ts
 import { createAnthropic } from "@ai-sdk/anthropic";
 import { createOpenAI } from "@ai-sdk/openai";
-function createAiClient(config) {
+function createAiClient(config, options = {}) {
   const { baseURL, apiKey, sdkType, authType } = config;
-  const useBearer = authType === "bearer";
+  const transport = { baseURL, fetch: options.fetch };
   if (sdkType === "openai") {
-    if (useBearer) {
-      return createOpenAI({
-        baseURL,
-        apiKey,
-        headers: { Authorization: `Bearer ${apiKey}` }
-      });
-    }
-    return createOpenAI({ baseURL, apiKey });
+    return createOpenAI({ ...transport, apiKey });
   }
-  if (useBearer) {
-    return createAnthropic({
-      baseURL,
-      apiKey,
-      headers: { Authorization: `Bearer ${apiKey}` }
-    });
-  }
-  return createAnthropic({ baseURL, apiKey });
+  return createAnthropic({
+    ...transport,
+    ...(authType === "bearer" ? { authToken: apiKey } : { apiKey })
+  });
 }
-function createAiModel(config) {
-  const client = createAiClient(config);
-  return client(config.model);
+function createAiModel(config, options = {}) {
+  const client = createAiClient(config, options);
+  return options.openaiApi === "chat" && "chat" in client
+    ? client.chat(config.model)
+    : client(config.model);
 }
 
 // src/server/helpers.ts
@@ -326,4 +317,3 @@ export {
   validateAiConfig,
   validateTestConfig
 };
-//# sourceMappingURL=server.js.map
\ No newline at end of file
